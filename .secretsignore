# .secretsignore
# Exclusion patterns for secrets scanning in CI pipeline
#
# This file documents patterns that are intentionally excluded from
# secrets scanning because they are false positives or test fixtures.

# Documentation files (may reference "password" in examples)
*.md
README*
docs/*

# Test fixtures and sample data
test/fixtures/*
test/integration/results/*
*.sample.json
*.template.json

# Configuration templates (placeholders only, no real secrets)
config/*.template.json

# Build and output artifacts
out/*
*.log

# Development tools
.vscode/*
.claude/*
.git/*
node_modules/*

# CI/CD configuration (contains the word "secret" in scanning logic)
.github/workflows/*

# PowerShell help comments (may contain parameter examples)
# Example: .PARAMETER Password - The user password
scripts/**/*.ps1:# .*password
scripts/**/*.ps1:# .*token
scripts/**/*.ps1:# .*secret

# Known false positives
# Add specific line-level exclusions here if needed:
# scripts/ops/example.ps1:42 - Contains "password" in documentation
